{
  "tasks": [
    {
      "id": "installer-clean-machine-interactive-validation",
      "type": "test",
      "status": "blocked",
      "risk": "medium",
      "priority": "high",
      "context": "Blocked in this workspace because it requires an interactive clean Windows Server or Windows 11 VM with installer privileges. Local validation completed on 2026-04-30: npm run bootstrap, npm run doctor, npm run build, npm run test, npm run pack, npm run publish and a controlled npm start smoke passed; setup EXE generation completed, and the local Windows service lifecycle check passed. Still needs manual clean-machine validation of Node.js prerequisite installation, per-machine install, service startup from the installed MSI/setup path, all-users desktop shortcut, admin reset code flow and uninstall cleanup."
    }
  ]
}
