{
  "project_name": "OnlyGANTT",
  "status": "active",
  "current_phase": "security-remediation",
  "last_updated": "2026-05-15T10:41:00+02:00",
  "production_target": true,
  "summary": "Windows-oriented Node/Express and React Gantt application with JSON-file persistence, optional LDAP, admin flows, Windows service host and WiX packaging.",
  "active_objective": "Continue post-audit remediation after disabling unsafe username-only login by default.",
  "assumptions": [
    "Audit was performed in a local Windows test workspace.",
    "The application code was not changed during the audit.",
    "Local or LDAP users without an assigned department keep legacy all-department scope until a stricter role/reparto model is defined."
  ],
  "decisions": [
    "Classify the project as Rischioso until server-side authorization and data-integrity risks are addressed.",
    "Keep audit output as root-level markdown and JSON artifacts for follow-up planning.",
    "Department project read, export, save, import and upload now require a valid user/admin session; password-protected departments require per-session password verification before data access.",
    "Keep WiX setup dialog and banner bitmaps text-free in areas where standard WiX dialogs draw their own title, description and body controls.",
    "Use #admin as the client deep link for desktop shortcuts that should open the Admin login tab.",
    "Bump package version to 1.0.1 so rebuilt MSI/setup artifacts are applied as a new installer release.",
    "Suppress WiX ICE38 for the per-machine DesktopFolder URL shortcut components; install validation verifies the files are created on the public desktop.",
    "Username-only standard login is disabled by default; legacy behavior requires explicit ONLYGANTT_ALLOW_USERNAME_ONLY_LOGIN compatibility opt-in when LDAP is disabled and no local users exist."
  ],
  "current_tasks": [],
  "next_tasks": [
    "Add range validation for system configuration values.",
    "Plan persistence hardening or migration away from fragile JSON writes."
  ],
  "blockers": [],
  "risks": [
    "Enabling ONLYGANTT_ALLOW_USERNAME_ONLY_LOGIN intentionally restores legacy username-only sessions and should be limited to controlled compatibility or demo deployments.",
    "Users with no department assignment retain all-department scope until role-based department authorization is specified.",
    "JSON file persistence is not safe against multiprocess writes or crash windows."
  ],
  "dependencies": {
    "runtime": [
      "Node.js >=20",
      "PowerShell 7+",
      ".NET SDK for service host build"
    ],
    "development": [
      "npm",
      "esbuild",
      "WiX 3.14.1 for packaging"
    ],
    "external_services": [
      "Optional LDAP server",
      "Optional HTTPS certificate files"
    ]
  },
  "quality_gates": {
    "format": "not_available",
    "lint": "not_available",
    "typecheck": "not_available",
    "tests": "passed",
    "build": "passed",
    "security_review": "completed_with_high_risks"
  },
  "commands": {
    "install": "npm run bootstrap",
    "dev": "npm start",
    "test": "npm run test",
    "lint": "",
    "typecheck": "",
    "build": "npm run build",
    "start": "npm start"
  },
  "environment": {
    "os_target": "Windows",
    "shell": "PowerShell",
    "required_env_vars": [],
    "env_files": [
      "Data/config/system-config.json",
      "Data/config/system-config.local.json"
    ]
  },
  "last_completed_work": "Disabled username-only standard login by default behind ONLYGANTT_ALLOW_USERNAME_ONLY_LOGIN, updated client auth handling, documentation and regression coverage, then passed npm run test and npm run build.",
  "open_questions": [
    "What role/reparto authorization model is required for local and LDAP users?",
    "Should generated tool caches remain inside the repository workspace?"
  ],
  "handoff_notes": "AUD-001 backend authorization is implemented and covered by regression tests. Username-only standard login is now disabled by default and requires ONLYGANTT_ALLOW_USERNAME_ONLY_LOGIN for legacy compatibility. MSI banner/dialog assets are adjusted, installer URL shortcuts use WiX InternetShortcut targeting DesktopFolder, package version is 1.0.1, real MSI install validation confirmed public desktop shortcuts are created, and package/test gates pass. Continue with first department password setup protection, config range validation and persistence hardening before treating the system as production-ready."
}
