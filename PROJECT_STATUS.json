{
  "tasks": [
    {
      "id": "installer-clean-machine-interactive-validation",
      "type": "test",
      "status": "blocked",
      "risk": "medium",
      "priority": "high",
      "context": "Blocked in this workspace because it requires an interactive clean Windows Server or Windows 11 VM. Local validation completed on 2026-04-29: npm audit --omit=dev, npm run build, npm run test and npm run pack passed, including native Windows service lifecycle validation and setup EXE generation. Still needs manual VM validation of Node.js prerequisite installation, per-machine install, service startup, all-users desktop shortcut, admin reset code flow and uninstall cleanup."
    }
  ]
}
