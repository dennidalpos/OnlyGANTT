# OnlyGANTT

OnlyGANTT is a Windows-oriented web application for managing project Gantt charts by department. It runs as a Node.js/Express server, serves a bundled React browser UI, and supports optional Windows service and x64 MSI packaging.

It is not a standalone desktop app. The supported product surface is a web UI hosted by the local/server Node process.

## Features

- Interactive Gantt chart with projects, phases, milestones, percentages, alerts and Italian holidays.
- Department-based project files under `Data/reparti/`.
- Multi-user editing locks with persisted lock state under `Data/config/locks.json`.
- Local users, admin login, department password protection and password reset flows.
- Optional LDAP authentication with local fallback support.
- Admin settings import/export with LDAP bind password kept out of the versioned seed config.
- JSON import/export for department projects.
- Local React bundle generated by `esbuild`, served from `artifacts/build/client/`.
- Windows service support through NSSM.
- x64 MSI packaging through WiX 3.14.1, with Node.js as an external prerequisite.

## Requirements

- Windows.
- PowerShell 7+ or Windows PowerShell compatible with the repository scripts.
- Node.js 18 or newer.
- npm.
- Administrator privileges only for Windows service install/uninstall and MSI lifecycle validation.

## Quick Start

```powershell
npm run bootstrap
npm run doctor
npm start
```

The app starts on `http://localhost:3000` by default. `npm start` runs `scripts/compile.ps1` first, then starts `src/server/server.js`.

## Commands

| Command | Purpose |
| --- | --- |
| `npm run bootstrap` | Install dependencies with `npm ci`. |
| `npm run doctor` | Check Node/npm, required files, installed dependencies and packaging inputs. |
| `npm run compile` | Build the browser bundle and runtime manifest. |
| `npm run build` | Run `doctor` and `compile`. |
| `npm run test` | Run the canonical regression checks. |
| `npm run pack` | Build the x64 MSI under `artifacts/packages/msi/`. |
| `npm run publish` | Copy existing package artifacts to `artifacts/publish/local/`. |
| `npm run clean` | Remove managed artifacts, stale local outputs and runtime-only data files. |

MSI lifecycle validation is available when the environment can install and remove machine-wide packages:

```powershell
npm run pack -- -RunMsiLifecycleValidation
```

Automated environments can also set `ONLYGANTT_RUN_MSI_TESTS=true`. This repository currently has no versioned GitHub Actions workflow.

## Windows Service

The service scripts are in `scripts/windows/` and use `tools/nssm/`:

```powershell
npm run service:install
npm run service:start
npm run service:stop
npm run service:uninstall
```

The manual service path uses the repository or install root as the application directory. The MSI path installs under `C:\Program Files\OnlyGANTT` and sets `ONLYGANTT_DATA_DIR` to the installed `Data` directory.

## Packaging

`npm run pack` builds `OnlyGantt-<version>-x64.msi`. The MSI is x64-only by design: the WiX source uses `ProgramFiles64Folder`, `Platform="x64"` and NSSM `win64`.

The installer:

- requires Node.js to be installed separately;
- installs the server, public assets, dependencies and Windows service configuration;
- uses the Windows brand icon from `src/public/brand/onlygantt.ico`;
- stores package outputs under `artifacts/packages/msi/`.

## Project State

Supported today:

- web runtime on Windows;
- canonical build/test/package scripts through npm;
- Windows service install/uninstall scripts;
- x64 MSI build and lifecycle tests;
- deterministic brand asset generation.

Not currently present:

- desktop shell packaging;
- x86 MSI packaging;
- shortcut creation;
- versioned CI workflow.

Open residual work is tracked in [`PROJECT_STATUS.json`](PROJECT_STATUS.json).

## Technical Docs

- [Script reference](docs/scripts.md)
- [Brand assets](docs/brand-assets.md)

## License

This repository is marked `UNLICENSED` in `package.json` and includes a `LICENSE` file. Confirm distribution policy before publishing artifacts.
